class Trip {
 constructor(tripData) {
  this.tripData = tripData,
  this.id = tripData.id,
  this.tripID = tripData.tripID,
  this.travlers = tripID.travelers,
  this.date = tripData.date,
  this.status = tripData.status,
  this.suggestedActivities = []
 }
}

export default Trip;
